# ColorMirror_Re(MVP版)

## サービス概要（元アプリより引用）
- その日の気分と相関する「色」を通じて、1日をポジティブに始められる記録アプリです。
- 登録した気分に対応した感情をもとにAIがパーソナライズコメントを生成します。
- コメントは“自分だけの手紙”として記録され、自己対話を促します。

## 既存ポートフォリオ「ColorMirror」[Rails(turbo) + JS] をReact + vite + TypeScript + Rails(APIモード)でリプレイスしてみた所感
 - 既存のアプリで出来なかったフロントエンド、バックエンドを分割してモノレポ構成での開発を経験することができた
  →__フロントエンド/バックエンドを分割し、GitHub ActionsでCI/CDを別々に管理して開発を進める経験を試すことができた__
   - 責務分離
   - API設計
   - ディレクトリ設計
   - 認証の実装   
  → __これらのRails単体では意識してない領域について触れることができた__
   - 状態管理
   - サーバーの状態管理
   - APIの共通化
   - TypeScriptによる型定義   
  → __これらのReact領域にも触れ、キャッチアップしつつ１通りのモノレポ構成によるSPA開発体験とすることができた__  
 >***今回、単純なリプレイス開発でなく、Rails側が持っていた責務をReactに移したらどのように分離、実装するのかを理解する開発にすることができた***
 - 技術的な所感は以下「開発背景」にて詳しく語ることとする

## 開発背景
  - リプレイス元のアプリ「ColorMirror」の開発を終えてみて考えたこと
    - アプリで得られるユーザーの体験が、form送信を起点に、
    - 気分の入力
    - colorの生成
    - 天気の自動取得
    - AIのコメント生成
    - CRUD  
    → __これらを直線的な体験として繋ぐ構成となっていた。__  

      しかし、その体験を実現させるためには
      - 複数のモデルの連携
      - JSによるDOM操作
      - 非同期通信
      - Railsのcontrollerへの直接API化
      - turboによるUIの更新
      - 状態の管理  

      __これらを1つの画面に複合させる必要があった。__  
  
      __結果として、自然とアプリのディレクトリ構成がコンポーネントライクに寄せてしまい、UIを担うのがJavaScriptとRails(turbo)との間で曖昧になってしまっている。__  

      __よって、***React***を使い、フロントとサーバーとで責務を分割して、複合的になっているUI層を整理した形でアプリを作ってみたいと考えた。__
  
      __そうすることで、RailsをAPIサーバーとして立て、データの返却や、認証の処理に集中させることで、責務の分割が明確化された状態で機能の追加が行える構成になると考えた。__
  
  - JavaScript / Rails[turbo]中心構成をReact化した理由
    - 
  - いざRailsでアプリを作ってみて、モダンフロントエンド構成へ発展できる設計だったこと


---
## おおまかなアプリの処理フロー
```
mood選択
↓
color生成 // 外部API
↓
color選択
↓
AIコメント生成  // 外部API
↓
Color + Response保存
↓
履歴表示
```

## 👩‍💻 機能選定

### 認証機能
- devise_token_auth によるログイン認証
  - ログイン
  - ログアウト
  - 認証状態による画面制御

### 基幹機能
- 気分の選択
- 選択した気分を元にした color 生成
- 生成された color 候補から1色を選択
- 選択した color と mood を元に AIコメントを生成
- color と AIコメントを関連付けて保存
- 保存した履歴の一覧表示
- 履歴の削除

### UI/UX
- toast による操作結果通知
- loading 表示
- 生成 / 再生成 / 保存の状態制御
- 最低限のレスポンシブ対応

---

### 今後の拡張予定

### 天気API連携
- 登録したユーザー情報を元に天気情報を自動取得
- 天気情報を記録データに組み込み保存
- 天気情報を AIコメント生成に反映

### 振り返り機能
- カレンダー形式での記録可視化
- mood / color の傾向分析
- 過去の記録を元にした AIコメント生成

### 通知・継続支援
- LINE またはメールによるリマインド
- 記録習慣を支援する通知機能

## 🔧 技術スタック

| 分類 | 技術 | 補足 |
|---|---|---|
| フロントエンド | React / Vite / TypeScript | SPA構成 |
| UI | Tailwind CSS / shadcn/ui | コンポーネント・スタイル設計 |
| 状態管理 | Zustand | 認証状態などのグローバル状態管理 |
| サーバー状態管理 | TanStack Query | APIデータ取得・キャッシュ・再取得制御 |
| バックエンド | Ruby on Rails 8.x APIモード | APIサーバー |
| 認証 | devise / devise_token_auth | トークンベース認証 |
| データベース | PostgreSQL | 本番 / 開発共通 |
| 外部API | OpenAI API | color生成・AIコメント生成 |
| インフラ | Vercel / Render | フロントエンド / バックエンドの分離デプロイ |
| CI/CD | GitHub Actions | フロント / バックのCI/CD制御 |

## 設計意図（工夫）

### Zustandによる認証状態管理（技術記事のリンク）

### TanStack Queryの活用（技術記事のリンク）

### Rails API側の設計（技術記事のリンク）

### リプレイスにあたって苦労した点・学び

### ※ 注意事項
**Render無料枠を利用している**
  > *初回起動に遅延が生じていることのお断り*
