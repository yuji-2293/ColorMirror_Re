  - form送信
    - STEP式のformだが、1つのファイルに複数のSTEPが混在している
    - 天気を表示するUI
    - 色の生成を行うformとUI
    - ライブラリのswiperの制御
    - 外部APIへのロジックがJSのDOM操作由来
      => 責務が違う領域が混ざっていて、formに任せる責務が大きすぎる（UI表示、外部API通信、ライブラリ制御を一手に担ってる）
      ```
      formによるReact化による利点
      - 色生成
      - 色の選択
      - コメント生成
      のように１STEP = 1機能(= feature)に分割
      それぞれ、components/hooks/api : UI/ロジック/API通信 によって分割することで
      - 可読性の向上
      - 責務分離
      ができるようになった
      ```
